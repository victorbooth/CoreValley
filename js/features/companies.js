  function installPaydaysCashFlowAndCompanies(){
    const todayIso=()=>new Date().toISOString().slice(0,10),currentMonth=()=>todayIso().slice(0,7),escapeHtml=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
    state.householdUsers.forEach((user,index)=>{user.jobs=user.jobs||[{id:'job-'+user.id+'-sample',employer:'Sample Employer '+(index+1),employerAddress:'123 Example Street, Sample City',payType:index?'hourly':'salary',annualSalary:index?0:72000,hourlyWage:index?28.5:0,typicalWeeklyHours:index?40:0,payFrequency:index?'semimonthly':'biweekly',nextPayDate:index?'2026-08-15':'2026-08-14',active:true}]});
    state.companies=state.companies||[
      {id:'company-holdings',name:'Sample Holdings LLC',entityType:'Parent company',parentId:'',description:'Demonstration parent entity for the household company structure.',incomeYtd:42000,taxesPaid:false,taxYear:new Date().getFullYear(),status:'active'},
      {id:'company-services',name:'Sample Services LLC',entityType:'Subsidiary',parentId:'company-holdings',description:'Demonstration operating subsidiary.',incomeYtd:26500,taxesPaid:true,taxesPaidDate:new Date().getFullYear()+'-03-01',taxYear:new Date().getFullYear(),status:'active'},
      {id:'company-property',name:'Sample Property LLC',entityType:'Subsidiary',parentId:'company-holdings',description:'Demonstration property subsidiary.',incomeYtd:11800,taxesPaid:false,taxYear:new Date().getFullYear(),status:'active'},
      {id:'company-trust',name:'Sample Family Trust',entityType:'Trust',parentId:'',description:'Standalone demonstration trust record.',incomeYtd:7400,taxesPaid:true,taxesPaidDate:new Date().getFullYear()+'-02-20',taxYear:new Date().getFullYear(),status:'active'}
    ];
    state.companyTaxReminder=state.companyTaxReminder||{enabled:true,cadence:'weekly',lastSentDate:'',deliveryStatus:'backend-required'};
    if(!(state.entries||[]).length&&!state.cashFlowDemoSeeded){const bank=state.accounts.find(account=>account.type!=='creditCard');const month=currentMonth(),priorDate=new Date(month+'-01T12:00');priorDate.setMonth(priorDate.getMonth()-1);const prior=priorDate.toISOString().slice(0,7);state.entries.push({id:'cash-demo-income',accountId:bank?.id,kind:'income',date:month+'-01',amount:7600,category:'Paycheck',notes:'Sample household income'},{id:'cash-demo-grocery',accountId:bank?.id,kind:'expense',date:month+'-04',amount:286.42,category:'Groceries',notes:'Sample grocery spending'},{id:'cash-demo-out-food',accountId:bank?.id,kind:'expense',date:month+'-06',amount:94.75,category:'Dining',notes:'Sample restaurant and food delivery'},{id:'cash-demo-prior-grocery',accountId:bank?.id,kind:'expense',date:prior+'-12',amount:512.18,category:'Groceries',notes:'Sample prior-month groceries'},{id:'cash-demo-prior-out-food',accountId:bank?.id,kind:'expense',date:prior+'-19',amount:178.35,category:'Dining',notes:'Sample prior-month restaurant spending'});state.cashFlowDemoSeeded=true}
    document.head.insertAdjacentHTML('beforeend',`<style>
      .payday-banner{display:grid;grid-template-columns:auto minmax(0,1fr);gap:13px;align-items:center;background:linear-gradient(105deg,var(--brand),var(--brand-2));color:#fff;border:0}.payday-banner .payday-icon{width:40px;height:40px;border-radius:12px;background:#ffffff1c;display:grid;place-items:center}.payday-list{display:flex;gap:10px;flex-wrap:wrap;margin-top:7px}.payday-chip{background:#ffffff18;border:1px solid #ffffff2f;border-radius:20px;padding:6px 10px;font-size:13px}.payday-chip strong{color:#fff}.cash.cashflow-dashboard{display:block;padding:18px}.cashflow-head{display:flex;justify-content:space-between;align-items:center;gap:14px;margin-bottom:15px}.cashflow-head select{min-width:190px}.cashflow-kpis{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-bottom:14px}.cashflow-kpi{background:var(--wash);border:1px solid var(--line);border-radius:12px;padding:11px}.cashflow-kpi span{display:block;color:var(--muted);font-size:13px;font-weight:700}.cashflow-kpi strong{font:800 20px Manrope}.cashflow-legend{display:flex;gap:16px;align-items:center;font-size:13px;margin-bottom:9px}.legend-dot{width:10px;height:10px;border-radius:50%;display:inline-block;margin-right:5px}.cashflow-category-list{display:grid;gap:9px}.cashflow-category{display:grid;grid-template-columns:150px minmax(160px,1fr) 110px;gap:12px;align-items:center}.cashflow-category>span{font-weight:700}.cashflow-stack{height:16px;border-radius:9px;background:#edf1ef;overflow:hidden;display:flex}.cashflow-paid{background:#3f9b70}.cashflow-expected{background:#d9685d}.cashflow-values{text-align:right;font-size:12px;color:var(--muted)}
      .companies-page{display:none}.companies-page.active{display:block}.company-alert{background:#fff0ee;border:1px solid #e9aaa2;color:#7f261d}.company-alert i{color:#bd4033}.company-kpis{grid-template-columns:repeat(4,minmax(0,1fr));margin-bottom:16px}.company-layout{display:grid;grid-template-columns:minmax(0,1.35fr) minmax(340px,.65fr);gap:16px}.company-list{display:grid;gap:12px}.company-card{position:relative}.company-card.overdue{border-top:5px solid #c74739}.company-card .inventory-actions{margin-top:13px}.company-org{overflow:auto}.org-forest{display:flex;gap:28px;align-items:flex-start;min-width:max-content;padding:10px}.org-tree{display:flex;flex-direction:column;align-items:center;gap:15px;position:relative}.org-node{min-width:185px;max-width:230px;text-align:center;border:1px solid var(--line);background:var(--wash);border-radius:13px;padding:12px}.org-node strong{display:block}.org-children{display:flex;gap:15px;align-items:flex-start;position:relative;padding-top:14px}.org-children:before{content:'';position:absolute;top:0;left:50%;width:1px;height:14px;background:var(--line)}.company-dashboard-card{grid-column:1/-1}.company-dashboard-card.overdue{border-top:5px solid #c74739}.job-groups{display:grid;gap:13px}.job-group{border:1px solid var(--line);border-radius:13px;padding:13px}.job-row{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:12px;padding:10px 0;border-bottom:1px solid var(--line)}.job-row:last-child{border:0}.job-actions{display:flex;gap:7px;align-items:center}.backend-state{display:inline-flex;align-items:center;gap:6px;border-radius:20px;background:#fff4df;color:#815a14;padding:6px 10px;font-size:12px;font-weight:800}
      @media(max-width:1100px){.company-layout{grid-template-columns:1fr}.company-kpis{grid-template-columns:repeat(2,1fr)}}@media(max-width:720px){.cashflow-kpis,.company-kpis{grid-template-columns:1fr}.cashflow-category{grid-template-columns:1fr}.cashflow-values{text-align:left}.cashflow-head{align-items:flex-start;flex-direction:column}.job-row{grid-template-columns:1fr}}
    </style>`);
    const navButton=document.createElement('button');navButton.dataset.page='Companies';navButton.innerHTML='<i data-lucide="building-2"></i><span>Companies</span>';const settingsNav=$('#nav button[data-page="Settings"]');if(!$('#nav button[data-page="Companies"]'))settingsNav.before(navButton);
    const companyPage=document.createElement('section');companyPage.id='companiesPage';companyPage.className='companies-page';companyPage.innerHTML=`<div class="module-intro"><div><h2 style="font-size:24px">Companies</h2><div class="module-copy">Household companies, trusts, ownership relationships, income, and annual business-tax status.</div></div><button class="primary-btn" id="newCompanyBtn"><i data-lucide="plus"></i> New entity</button></div><div id="companyPageAlert"></div><div class="grid company-kpis"><article class="card"><div class="kpi-label">Active entities</div><strong id="companyEntityCount">0</strong><div class="sub">Companies and trusts</div></article><article class="card"><div class="kpi-label">Income recorded YTD</div><strong id="companyIncomeYtd">$0</strong><div class="sub">Across active entities</div></article><article class="card"><div class="kpi-label">Taxes still open</div><strong id="companyTaxesOpen">0</strong><div class="sub">For the current tax year</div></article><article class="card"><div class="kpi-label">Email reminder delivery</div><strong style="font-size:16px">Backend required</strong><div class="sub">Weekly after March 15 when overdue</div></article></div><div class="company-layout"><article class="card"><div class="card-head"><div><h2>Entity records</h2><div class="card-note">Income and filing status remain editable for each company or trust.</div></div></div><div class="company-list" id="companyList"></div></article><article class="card company-org"><div class="card-head"><div><h2>Household company structure</h2><div class="card-note">Parent, subsidiary, trust, and standalone relationships.</div></div></div><div class="org-forest" id="companyOrgChart"></div></article></div>`;$('#placeholder').before(companyPage);
    const dateLabel=value=>value?new Date(value+'T12:00').toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric'}):'Not set';
    const nextPayday=job=>{if(!job.active||!job.nextPayDate)return null;let date=new Date(job.nextPayDate+'T12:00'),today=new Date(todayIso()+'T12:00');while(date<today){if(job.payFrequency==='weekly')date.setDate(date.getDate()+7);else if(job.payFrequency==='biweekly')date.setDate(date.getDate()+14);else if(job.payFrequency==='semimonthly')date.setDate(date.getDate()+15);else{const day=date.getDate();date.setMonth(date.getMonth()+1);date.setDate(Math.min(day,new Date(date.getFullYear(),date.getMonth()+1,0).getDate()))}}return date.toISOString().slice(0,10)};
    const renderPaydayBanner=()=>{let banner=$('#dashboardPaydayBanner');if(!banner){banner=document.createElement('article');banner.id='dashboardPaydayBanner';banner.className='card payday-banner';$('#dashboard .alert')?.before(banner)}const rows=state.householdUsers.flatMap(user=>(user.jobs||[]).map(job=>({user,job,date:nextPayday(job)}))).filter(row=>row.date).sort((a,b)=>a.date.localeCompare(b.date));const eachUser=state.householdUsers.map(user=>rows.find(row=>row.user.id===user.id)).filter(Boolean);banner.innerHTML=`<div class="payday-icon"><i data-lucide="calendar-heart"></i></div><div><strong>Next household paydays</strong><div class="payday-list">${eachUser.map(row=>`<span class="payday-chip"><strong>${escapeHtml(row.user.preferredName||row.user.displayName)}</strong> · ${escapeHtml(row.job.employer)} · ${dateLabel(row.date)}</span>`).join('')||'<span class="payday-chip">Add a job and pay schedule in Household & Users.</span>'}</div></div>`};
    const annualJobIncome=job=>job.active===false?0:job.payType==='hourly'?Number(job.hourlyWage||0)*Number(job.typicalWeeklyHours||0)*52:Number(job.annualSalary||0),ownerMatches=(owner,user)=>[user.displayName,user.preferredName].filter(Boolean).some(name=>String(owner||'').toLowerCase()===String(name).toLowerCase()),renderPredictedIncome=()=>{const card=$$('#dashboard .kpis .kpi')[3];if(!card)return;card.id='dashboardPredictedIncome';const activePositions=(state.positions||[]).filter(position=>position.status!=='closed'),accountForPosition=position=>state.investmentAccounts.find(account=>account.id===position.accountId),people=state.householdUsers.map(user=>{const wages=(user.jobs||[]).reduce((sum,job)=>sum+annualJobIncome(job),0),investmentIncome=activePositions.filter(position=>ownerMatches(accountForPosition(position)?.owner||position.owner,user)).reduce((sum,position)=>sum+positionIncome(position),0),bankInterest=state.accounts.filter(account=>account.type!=='creditCard'&&ownerMatches(account.owner,user)).reduce((sum,account)=>sum+Number(account.balance||0)*Number(account.apy||0)/100,0),passive=investmentIncome+bankInterest;return{user,wages,passive,total:wages+passive}}),wages=people.reduce((sum,person)=>sum+person.wages,0),investmentIncome=activePositions.reduce((sum,position)=>sum+positionIncome(position),0),bankInterest=state.accounts.filter(account=>account.type!=='creditCard').reduce((sum,account)=>sum+Number(account.balance||0)*Number(account.apy||0)/100,0),passive=investmentIncome+bankInterest,total=wages+passive,assignedPassive=people.reduce((sum,person)=>sum+person.passive,0),jointPassive=Math.max(0,passive-assignedPassive),hours=Number(state.appSettings?.annualWorkHours||2080);card.innerHTML=`<div class="kpi-label"><span>Predicted annual income · Household</span><span class="kpi-icon"><i data-lucide="sparkles"></i></span></div><div class="kpi-value">${money(total)}</div><div class="income-kpi-breakdown"><span><strong>${money(wages)}</strong> wages</span><span><strong>${money(passive)}</strong> passive</span></div><div class="income-person-grid">${people.map(person=>`<div><span>${escapeHtml(person.user.preferredName||person.user.displayName)}</span><strong>${money(person.total)}</strong><small>${money(person.wages)} wages · ${money(person.passive)} passive</small></div>`).join('')}${jointPassive?`<div><span>Joint / household assets</span><strong>${money(jointPassive)}</strong><small>Passive income not assigned to one person</small></div>`:''}</div><div class="sub"><span class="positive trend">${money(passive/hours)}/hr</span> household passive hourly wage</div>`;lucide.createIcons()};
    const recurringMonthly=item=>item.frequency==='weekly'?Number(item.amount)*52/12:item.frequency==='quarterly'?Number(item.amount)/3:item.frequency==='annual'?Number(item.amount)/12:Number(item.amount);
    const categoryForEntry=entry=>{const text=((entry.category||'')+' '+(entry.notes||'')).toLowerCase();if(/grocery|groceries|supermarket/.test(text))return'Groceries';if(/restaurant|dining|fast food|delivery|doordash|uber eats|out food/.test(text))return'Out Food';return entry.category||'Other spending'};
    const renderCashFlow=()=>{const cash=$('#dashboard .cash');if(!cash)return;cash.classList.add('cashflow-dashboard');const charts=$('#dashboard .charts');if(charts&&cash.nextElementSibling!==charts)charts.before(cash);const selected=currentMonth(),months=Array.from({length:18},(_,index)=>{const date=new Date();date.setDate(1);date.setMonth(date.getMonth()-index);return date.toISOString().slice(0,7)});cash.innerHTML=`<div class="cashflow-head"><div><h2>Monthly cash flow</h2><div class="card-note">Paid activity versus fixed amounts still expected for the selected month.</div></div><select id="dashboardCashMonth">${months.map(month=>`<option value="${month}" ${month===selected?'selected':''}>${new Date(month+'-01T12:00').toLocaleDateString('en-US',{month:'long',year:'numeric'})}</option>`).join('')}</select></div><div id="dashboardCashBody"></div>`;const draw=month=>{const groups={},add=(name,paid=0,expected=0)=>{groups[name]=groups[name]||{paid:0,expected:0};groups[name].paid+=Number(paid)||0;groups[name].expected+=Number(expected)||0};(state.entries||[]).filter(entry=>entry.date?.startsWith(month)&&entry.kind==='expense').forEach(entry=>add(categoryForEntry(entry),entry.amount,0));(state.payments||[]).filter(payment=>payment.date?.startsWith(month)).forEach(payment=>add(state.loans.some(loan=>loan.name===payment.name)?'Loan payments':'Credit card payments',payment.amount,0));const isCurrent=month===currentMonth();if(isCurrent)(state.recurringItems||[]).filter(item=>item.active!==false&&item.status!=='archived').forEach(item=>{const name=item.type==='subscription'?'Subscriptions':item.type==='utility'?'Utilities':'Charitable giving',already=(state.entries||[]).some(entry=>entry.date?.startsWith(month)&&String(entry.notes||'').toLowerCase().includes(String(item.name||'').toLowerCase()));add(name,0,already?0:recurringMonthly(item))});if(isCurrent){state.accounts.filter(account=>account.type==='creditCard').forEach(account=>{const paid=(state.payments||[]).filter(payment=>payment.date?.startsWith(month)&&payment.name===account.nickname).reduce((sum,payment)=>sum+Number(payment.amount||0),0);add('Credit card payments',0,Math.max(0,Number(account.minimumPayment||0)-paid))});state.loans.forEach(loan=>{const paid=(state.payments||[]).filter(payment=>payment.date?.startsWith(month)&&payment.name===loan.name).reduce((sum,payment)=>sum+Number(payment.amount||0),0);add('Loan payments',0,Math.max(0,Number(loan.payment||0)-paid))})}const rows=Object.entries(groups).filter(([,value])=>value.paid||value.expected).sort((a,b)=>b[1].paid+b[1].expected-(a[1].paid+a[1].expected)),paid=rows.reduce((sum,[,value])=>sum+value.paid,0),expected=rows.reduce((sum,[,value])=>sum+value.expected,0),income=(state.entries||[]).filter(entry=>entry.date?.startsWith(month)&&entry.kind==='income').reduce((sum,entry)=>sum+Number(entry.amount||0),0),max=Math.max(1,...rows.map(([,value])=>value.paid+value.expected));$('#dashboardCashBody').innerHTML=`<div class="cashflow-kpis"><div class="cashflow-kpi"><span>Income recorded</span><strong>${money(income)}</strong></div><div class="cashflow-kpi"><span>Paid / recorded</span><strong>${money(paid)}</strong></div><div class="cashflow-kpi"><span>Expected fixed amounts</span><strong>${money(expected)}</strong></div></div><div class="cashflow-legend"><span><i class="legend-dot" style="background:#3f9b70"></i>Paid / recorded</span><span><i class="legend-dot" style="background:#d9685d"></i>Expected / not yet paid</span></div><div class="cashflow-category-list">${rows.map(([name,value])=>`<div class="cashflow-category"><span>${escapeHtml(name)}</span><div class="cashflow-stack"><i class="cashflow-paid" style="width:${value.paid/max*100}%"></i><i class="cashflow-expected" style="width:${value.expected/max*100}%"></i></div><div class="cashflow-values">${money(value.paid)} paid${value.expected?' · '+money(value.expected)+' due':''}</div></div>`).join('')||'<div class="sub">No cash-flow activity is recorded for this month.</div>'}</div>`};$('#dashboardCashMonth').onchange=event=>draw(event.target.value);draw(selected)};
    const companyOverdue=company=>company.status!=='archived'&&!company.taxesPaid&&new Date()>new Date((company.taxYear||new Date().getFullYear())+'-03-15T23:59:59');
    const orgBranch=company=>{const children=state.companies.filter(item=>item.status!=='archived'&&item.parentId===company.id);return`<div class="org-tree"><div class="org-node"><strong>${escapeHtml(company.name)}</strong><span class="sub">${escapeHtml(company.entityType)}</span></div>${children.length?`<div class="org-children">${children.map(orgBranch).join('')}</div>`:''}</div>`};
    const openCompanyEditor=id=>{const company=id?state.companies.find(item=>item.id===id):null;openSimpleData(company?'Edit entity':'New company or trust','Track ownership structure, derived income, description, and annual tax status.',`<div class="field"><label>Entity name</label><input id="companyName" value="${escapeHtml(company?.name||'')}" required></div><div class="field"><label>Entity type</label><select id="companyType">${['Parent company','Subsidiary','Standalone company','Trust','Other'].map(type=>`<option ${company?.entityType===type?'selected':''}>${type}</option>`).join('')}</select></div><div class="field"><label>Parent entity</label><select id="companyParent"><option value="">None / standalone</option>${state.companies.filter(item=>item.id!==id&&item.status!=='archived').map(item=>`<option value="${item.id}" ${company?.parentId===item.id?'selected':''}>${escapeHtml(item.name)}</option>`).join('')}</select></div><div class="field"><label>Income recorded YTD</label><input id="companyIncome" type="number" min="0" step=".01" value="${company?.incomeYtd||0}"></div><div class="field full"><label>Description</label><textarea id="companyDescription">${escapeHtml(company?.description||'')}</textarea></div><div class="field"><label>Tax year</label><input id="companyTaxYear" type="number" min="2000" max="2100" value="${company?.taxYear||new Date().getFullYear()}"></div><div class="field"><label>Taxes paid date</label><input id="companyTaxDate" type="date" value="${company?.taxesPaidDate||''}"></div><div class="field full"><label><input id="companyTaxesPaid" type="checkbox" ${company?.taxesPaid?'checked':''}> Business taxes paid for this tax year</label></div>`,()=>{const record=company||{id:'company-'+Date.now(),status:'active'};Object.assign(record,{name:$('#companyName').value.trim(),entityType:$('#companyType').value,parentId:$('#companyParent').value,incomeYtd:Number($('#companyIncome').value),description:$('#companyDescription').value.trim(),taxYear:Number($('#companyTaxYear').value),taxesPaid:$('#companyTaxesPaid').checked,taxesPaidDate:$('#companyTaxDate').value});if(!company)state.companies.push(record);dataProvider.save(state);renderCompanies();renderCompanyDashboard();showToast(record.name+' saved.')})};
    const renderCompanies=()=>{const active=state.companies.filter(company=>company.status!=='archived'),overdue=active.filter(companyOverdue),recipients=state.householdUsers.filter(user=>user.email).map(user=>user.email);$('#companyEntityCount').textContent=active.length;$('#companyIncomeYtd').textContent=money(active.reduce((sum,company)=>sum+Number(company.incomeYtd||0),0));$('#companyTaxesOpen').textContent=active.filter(company=>!company.taxesPaid).length;$('#companyPageAlert').innerHTML=overdue.length?`<div class="alert company-alert"><i data-lucide="triangle-alert"></i><div><strong>${overdue.length} business tax filing${overdue.length===1?' is':'s are'} overdue</strong><div class="sub">Weekly email reminders are configured for ${recipients.length?recipients.map(escapeHtml).join(', '):'all household users once email addresses are added'}. Delivery begins after the Apps Script backend is connected.</div></div><span class="backend-state">Backend required</span></div>`:'';$('#companyList').innerHTML=active.map(company=>`<article class="company-card ${companyOverdue(company)?'overdue':''}"><div class="inventory-top"><div><h2>${escapeHtml(company.name)}</h2><div class="card-note">${escapeHtml(company.entityType)}${company.parentId?' · subsidiary of '+escapeHtml(state.companies.find(item=>item.id===company.parentId)?.name||'Unknown'):''}</div></div><span class="inventory-badge">${company.taxesPaid?'Taxes paid':'Taxes open'}</span></div><div class="inventory-value">${money(company.incomeYtd||0)}</div><div class="sub">Income recorded YTD</div><p>${escapeHtml(company.description||'No description entered.')}</p><div class="inventory-actions"><button class="secondary-btn" data-edit-company="${company.id}">Edit</button>${!company.taxesPaid?`<button class="primary-btn" data-paid-company="${company.id}">Mark taxes paid</button>`:''}<button class="danger-btn" data-delete-company="${company.id}">Delete</button></div></article>`).join('')||'<div class="sub">No company or trust records yet.</div>';$('#companyOrgChart').innerHTML=active.filter(company=>!company.parentId||!active.some(item=>item.id===company.parentId)).map(orgBranch).join('');$$('[data-edit-company]').forEach(button=>button.onclick=()=>openCompanyEditor(button.dataset.editCompany));$$('[data-paid-company]').forEach(button=>button.onclick=()=>{const company=state.companies.find(item=>item.id===button.dataset.paidCompany);company.taxesPaid=true;company.taxesPaidDate=todayIso();dataProvider.save(state);renderCompanies();renderCompanyDashboard();showToast(company.name+' taxes marked paid.')});$$('[data-delete-company]').forEach(button=>button.onclick=()=>{const company=state.companies.find(item=>item.id===button.dataset.deleteCompany);if(confirm('Permanently delete '+company.name+'? Subsidiaries will become standalone records.')){state.companies.filter(item=>item.parentId===company.id).forEach(item=>item.parentId='');state.companies=state.companies.filter(item=>item.id!==company.id);dataProvider.save(state);renderCompanies();renderCompanyDashboard()}});lucide.createIcons()};
    const renderCompanyDashboard=()=>{let card=$('#dashboardCompanyCard');const grid=$('#dashboardUpgrade');if(!card&&grid){card=document.createElement('article');card.id='dashboardCompanyCard';card.className='card company-dashboard-card';grid.append(card)}if(!card)return;const active=state.companies.filter(company=>company.status!=='archived'),overdue=active.filter(companyOverdue);card.classList.toggle('overdue',overdue.length>0);card.innerHTML=`<div class="card-head"><div><h2>Companies & trusts</h2><div class="card-note">${active.length} entities · ${money(active.reduce((sum,company)=>sum+Number(company.incomeYtd||0),0))} income recorded YTD</div></div><button class="link-btn" id="openCompaniesBtn">Open companies</button></div>${overdue.length?`<div class="alert company-alert" style="margin:10px 0 0"><i data-lucide="triangle-alert"></i><div><strong>${overdue.length} overdue business tax filing${overdue.length===1?'':'s'}</strong><div class="sub">Weekly reminder delivery is waiting for the secure backend.</div></div></div>`:`<div class="settings-status"><i data-lucide="badge-check"></i><div><strong>No overdue business tax filings</strong><div class="sub">Current entity records are clear.</div></div></div>`}`;$('#openCompaniesBtn').onclick=()=>setPage('Companies');lucide.createIcons()};
    const openJobEditor=(userId,id)=>{const user=state.householdUsers.find(item=>item.id===userId),job=id?(user.jobs||[]).find(item=>item.id===id):null;openSimpleData(job?'Edit job':'Add job','Each household member may have multiple jobs and independent pay schedules.',`<div class="field"><label>Household member</label><input value="${escapeHtml(user.displayName)}" disabled></div><div class="field"><label>Employer name</label><input id="jobEmployer" value="${escapeHtml(job?.employer||'')}" required></div><div class="field full"><label>Employer address</label><input id="jobAddress" value="${escapeHtml(job?.employerAddress||'')}"></div><div class="field"><label>Pay basis</label><select id="jobPayType"><option value="salary" ${job?.payType==='salary'?'selected':''}>Annual salary</option><option value="hourly" ${job?.payType==='hourly'?'selected':''}>Hourly wage</option></select></div><div class="field"><label>Annual salary</label><input id="jobSalary" type="number" min="0" step=".01" value="${job?.annualSalary||0}"></div><div class="field"><label>Base wage per hour</label><input id="jobHourly" type="number" min="0" step=".01" value="${job?.hourlyWage||0}"></div><div class="field"><label>Typical weekly hours</label><input id="jobHours" type="number" min="0" step=".25" value="${job?.typicalWeeklyHours||0}"></div><div class="field"><label>Pay schedule</label><select id="jobFrequency">${[['weekly','Weekly'],['biweekly','Every two weeks'],['semimonthly','Twice monthly'],['monthly','Monthly']].map(([value,label])=>`<option value="${value}" ${job?.payFrequency===value?'selected':''}>${label}</option>`).join('')}</select></div><div class="field"><label>Next known payday</label><input id="jobNextPay" type="date" value="${job?.nextPayDate||todayIso()}" required></div>`,()=>{const record=job||{id:'job-'+Date.now(),active:true};Object.assign(record,{employer:$('#jobEmployer').value.trim(),employerAddress:$('#jobAddress').value.trim(),payType:$('#jobPayType').value,annualSalary:Number($('#jobSalary').value),hourlyWage:Number($('#jobHourly').value),typicalWeeklyHours:Number($('#jobHours').value),payFrequency:$('#jobFrequency').value,nextPayDate:$('#jobNextPay').value});if(!job)user.jobs.push(record);dataProvider.save(state);renderJobSettings();renderPaydayBanner();renderPredictedIncome();showToast(record.employer+' saved.')})};
    const renderJobSettings=()=>{const panel=$('[data-settings-panel="household"]');if(!panel)return;let card=$('#householdJobsCard');if(!card){card=document.createElement('article');card.id='householdJobsCard';card.className='card section-gap';panel.append(card)}card.innerHTML=`<div class="card-head"><div><h2>Jobs & pay schedules</h2><div class="card-note">Configure multiple employers, compensation basis, and each person’s next payday.</div></div></div><div class="job-groups">${state.householdUsers.map(user=>`<div class="job-group"><div class="card-head"><div><strong>${escapeHtml(user.displayName)}</strong><div class="sub">${(user.jobs||[]).filter(job=>job.active).length} active job${(user.jobs||[]).filter(job=>job.active).length===1?'':'s'}</div></div><button class="secondary-btn" data-add-job="${user.id}">Add job</button></div>${(user.jobs||[]).map(job=>`<div class="job-row"><div><strong>${escapeHtml(job.employer)}</strong><div class="sub">${escapeHtml(job.employerAddress||'No employer address')} · ${job.payType==='hourly'?money(job.hourlyWage)+'/hour':money(job.annualSalary)+'/year'} · ${escapeHtml(job.payFrequency)} · next ${dateLabel(nextPayday(job))}</div></div><div class="job-actions"><button class="link-btn" data-edit-job="${user.id}|${job.id}">Edit</button><button class="danger-btn" data-delete-job="${user.id}|${job.id}">Delete</button></div></div>`).join('')||'<div class="sub">No jobs configured.</div>'}</div>`).join('')}</div>`;$$('[data-add-job]').forEach(button=>button.onclick=()=>openJobEditor(button.dataset.addJob));$$('[data-edit-job]').forEach(button=>button.onclick=()=>{const[userId,id]=button.dataset.editJob.split('|');openJobEditor(userId,id)});$$('[data-delete-job]').forEach(button=>button.onclick=()=>{const[userId,id]=button.dataset.deleteJob.split('|'),user=state.householdUsers.find(item=>item.id===userId),job=user.jobs.find(item=>item.id===id);if(confirm('Delete '+job.employer+' from '+user.displayName+'?')){user.jobs=user.jobs.filter(item=>item.id!==id);dataProvider.save(state);renderJobSettings();renderPaydayBanner();renderPredictedIncome()}});lucide.createIcons()};
    $('#newCompanyBtn').onclick=()=>openCompanyEditor();
    const baseSetPage=setPage;setPage=function(page){companyPage.classList.remove('active');baseSetPage(page);if(page==='Companies'){$$('.accounts-page,.loans-page,.payoff-page,.investments-page,.espp-page,.physical-assets-page,.settings-page,.operations-page,.funding-goals-page').forEach(element=>element.classList.remove('active'));$('#dashboard').classList.add('hidden');$('#placeholder').classList.remove('active');companyPage.classList.add('active');$('#pageTitle').textContent='Companies';renderCompanies()}if(page==='Dashboard'){renderPaydayBanner();renderCashFlow();renderCompanyDashboard();renderPredictedIncome()}if(page==='Settings')setTimeout(renderJobSettings,0)};
    $$('#nav button').forEach(button=>button.onclick=()=>setPage(button.dataset.page));
    document.head.insertAdjacentHTML('beforeend','<style>.income-kpi-breakdown{display:flex;gap:7px;flex-wrap:wrap;margin:7px 0}.income-kpi-breakdown span{background:var(--wash);border-radius:18px;padding:5px 8px;color:var(--muted);font-size:11px}.income-kpi-breakdown strong{color:var(--ink)}.income-person-grid{display:grid;gap:5px;margin:8px 0}.income-person-grid>div{display:grid;grid-template-columns:minmax(65px,auto) auto;gap:1px 8px;align-items:center;padding-top:5px;border-top:1px solid var(--line)}.income-person-grid span{font-size:11px;font-weight:800;color:var(--muted)}.income-person-grid strong{font-size:12px;text-align:right}.income-person-grid small{grid-column:1/-1;color:var(--muted);font-size:10px}</style>');CoreValleyModules.register('Companies',()=>renderCompanies());CoreValleyModules.register('Settings',()=>renderJobSettings());renderPaydayBanner();renderCashFlow();renderCompanyDashboard();renderPredictedIncome();dataProvider.save(state);lucide.createIcons();
  }
  installPaydaysCashFlowAndCompanies();
